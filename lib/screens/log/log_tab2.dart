import 'package:flutter/material.dart';

class LogTab2 extends StatelessWidget {
  const LogTab2({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}